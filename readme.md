First Server in Java

The client accepts command line string input, sends it to the server, which echoes it to
the client and then returned to the client.  

Commands:
Run java EchoServer first
Then run java EchoClient 


